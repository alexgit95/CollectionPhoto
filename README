## Pour tagger

docker tag collectionapi:latest alexyop/testaws:0.0.1
docker push alexyop/testaws:0.0.1


## Pour runner le container

avec les informations d'identification dans un fichier : credentials.aws

docker run -p 8888:8888 -v /c/Users/<USER WINDOWS>/.aws:/credentials/  collectionapi